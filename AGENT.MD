When processing a prompt from a user, Log the following information (activity id, category, title, summary, start, end, duration), write the start time as soon as you start with the request in a file called agentlog.txt in the root
